import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import Anchor from '.'

<Meta
  title="Components/Anchor"
  component={Anchor}
  argTypes={{
    color: { control: 'color' },
    text: { control: 'text' }
  }}
/>

# 🍎 Anchor

앵커입니다. 앵커는 `<a>` 태그 컴포넌트입니다.

## 예제

export const Template = (args) => <Anchor {...args}>{args.text}</Anchor>

<Canvas>
  <Story name="anchor" args={{ text: "앵커 - anchor" }}>
    {Template.bind()}
  </Story>
</Canvas>

## Props

|속성|타입|설명|
|:--|:--|:--|
|color|string|앵커의 색을 지정합니다. 올바른 색상 문자열이 아닐 경우 기본 앵커 색을 따릅니다.|
