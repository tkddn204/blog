import Styled from '@emotion/styled'

const Layout = Styled('div')`
  text-align: center;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-size: 10px;
`

export default Layout
